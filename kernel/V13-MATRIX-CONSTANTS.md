# V13 矩陣常量 K-18 至 K-27

狀態：`TESTED`
測定時間：2026-08-29
測定環境：協作者 Termux / `~/miya-check` / `main`
fixture：`tools/verify_crop_circle_v13_fix.py`
收據：`PASS=54  FAIL=0  SKIP=6`，`exit 0`

依 HARD RULES v1 第 10 條：`kernel/` 只接受 fixture 已驗証之結構。
本文件是本倉庫第一份準此條入 `kernel/` 的矩陣常量。

---

## 0. 結構定義（被測量的對象）

```
節點：1..13
核區：1..9      PUB：10..13

對位置換 P：
  1<->9  2<->8  3<->7  4<->6  5 不動
  10<->13  11<->12

鄰接表（第二版布線，P 守恆）：
N(1)={5,9,10}    N(9)={5,1,13}
N(2)={5,8,11}    N(8)={5,2,12}
N(3)={5,7,10}    N(7)={5,3,13}
N(4)={5,6,11}    N(6)={5,4,12}
N(5)={1,2,3,4,6,7,8,9}
N(10)={1,3,13}   N(13)={7,9,10}
N(11)={2,4,12}   N(12)={6,8,11}

M := 該簡圖的鄰接矩陣 over F3
     對稱、對角全零、元素只有 0 與 1
     M 不是 A - I
```

---

## 1. 常量表

| 編號 | 量 | 值 | 層 | 狀態 |
| --- | --- | --- | --- | --- |
| K-18 | `full_det mod 3` | **2** | 矩陣層 | `TESTED` |
| K-19 | `pub_det mod 3` | **1** | 矩陣層 | `TESTED` |
| K-20 | 邊數 | **22** | 圖層 | `TESTED` |
| K-21 | 度數和 | **44** | 圖層 | `TESTED` |
| K-22 | `deg(5)` | **8** | 圖層 | `TESTED` |
| K-23 | `deg(i)`，i 在 1-4,6-9 | **3** | 圖層 | `TESTED` |
| K-24 | `deg(j)`，j 在 10-13 | **3** | 圖層 | `TESTED` |
| K-25 | `P M P = M` 不符格數 | **0** | 矩陣層 | `TESTED` |
| K-26 | `row5` 核塊和 | **8** | 矩陣層 | `TESTED` |
| K-27 | Core-PUB 非零耦合數 | **8** | 矩陣層 | `TESTED` |

附：P 不變且可逆的 2x2 塊恰 **4** 種：`(0,1) (0,2) (1,0) (2,0)`（以 `[[a,b],[b,a]]` 的 (a,b) 記）。`TESTED`。

---

## 2. 三條合法棲息 —— 結案

```
det(M) != 0 mod 3      got = 2    [矩陣層]  PASS
pub_det != 0 mod 3     got = 1    [矩陣層]  PASS
core_pair_sum == 10    got = 10   [索引層]  PASS
```

**第三條必須帶層標籤。** 矩陣層的 `row5` 核塊和是 **8**，不是 10；舊矩陣（含對角 2）是 10。兩者皆非 5。
`5` 是索引層的不動點編號，不是任何矩陣行和。

→ `M 屬 GL(13, F3)`。三條同時成立。帶層標籤結案。

---

## 3. 建庫者預測對賬（先寫後驗）

| 編號 | 預測 | 實測 | 結果 |
| --- | --- | --- | --- |
| P-01 | M 對稱、對角全零、元素只有 0 與 1 | 同 | 命中 |
| P-02 | 邊數 22 | 22 | 命中 |
| P-03 | 度數和 44 | 44 | 命中 |
| P-04 | 核 3 / deg(5)=8 / PUB 3 | 同 | 命中 |
| P-05 | PMP=M 不符格數 0 | 0 | 命中 |
| P-06 | P 不變可逆 2x2 恰 4 種 | 4 | 命中 |
| P-07 | 五版本 det：0 / 2 / 2 / 2 / 1 | 同 | 命中 |
| P-08 | row5 核塊和 8 | 8 | 命中 |
| P-09 | pub_det 1 | 1 | 命中 |
| P-10 | **UNKNOWN，拒絕手猜** | **2** | 守住後測得 |

**P-01 到 P-09 零修正。P-10 守了四輪未手填。**

這是本倉庫第一次「手算預測先入庫、機器後核對、全數命中」。

---

## 4. 本文件不主張的事

| 項目 | 天花板 |
| --- | --- |
| `full_det = 2` 具有意義或美感 | 不主張。它只是一個 F3 行列式 |
| 這個 13x13 對應麦田圈、物理或意義 | `SYMBOLIC`，fixture 第十節 SKIP |
| 核區對角歸零是唯一可行方案 | 不主張。替代方案存在，但會讓對位律退回純索引層 |
| `C0_value` 型別 | 仍 `OPEN`，待拆為 `C0_index` 與 `C0_residue` |
| 這庫有 CI | 無。所有 exit 0 皆為人工在協作者手機上跑出 |
